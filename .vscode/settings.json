/* 
hide root .env file from vscode. default is {"python.envFile" : ".env"}
hiding prevent vscode from adding .env with highest priority to docker container,
interfering with environment overrides.
*/
{ "python.envFile": "" }
